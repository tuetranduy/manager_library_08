<%= form_for current_user.user_active_relationships.build , remote: true do |f| %>
  <div><%= hidden_field_tag :followed_id, @user.id %></div>
  <%= f.submit t("form.follow"), class: "btn btn-primary" %>
<% end %>
